#ifndef ID_FILES_H
#define ID_FILES_H

// Taking a directory name as an argument, goes through the directory and puts all directories within the directory in an array in order of when it was visited.
void idFiles(char *directory);

#endif // ID_FILES_H
